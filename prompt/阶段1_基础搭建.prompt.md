# 阶段1 Prompt（基础搭建）

```text
PROMPT START
你现在是资深 Python 全栈工程师，负责实现《三角洲行动子弹行情分析系统》的阶段1“基础搭建”。

【项目固定上下文】
- 项目目标：纯本地运行的子弹行情分析系统；不登录账号、不自动交易，只做行情分析与决策辅助
- 目标用户：普通玩家，强调安全与合规
- 默认技术栈：Python + Flask/FastAPI（二选一并说明理由）、SQLite、APScheduler
- 核心红线：
  1) 不获取账号密码
  2) 不执行任何自动化交易
  3) 数据默认本地存储，不上传云端

【本阶段目标（M1）】
完成从公开 API （比如https://df-api.apifox.cn/、游戏官方 API）拉取子弹价格并写入 SQLite，建立可扩展后端骨架与基础 REST API。

【输入材料】
- 计划书：项目概述、架构、技术栈、阶段一任务、数据库设计
- 当前仓库路径：{{REPO_PATH}}

【阶段范围（只做这些）】
1) 初始化项目结构（backend / database / scripts / tests 等）
2) 完成配置管理（环境变量与本地配置样例）
3) 设计并创建 SQLite Schema（ammo_info, price_history, holdings）
4) 实现 DataFetcher：从公开 API 获取并标准化数据
5) 实现入库流程：去重/时间戳/异常重试/失败日志
6) 提供基础 REST API：
   - GET /health
   - GET /api/ammo/latest
   - GET /api/ammo/{ammo_id}/history?days=7
7) 增加定时任务骨架（支持每小时抓取一次，可配置）
8) 补齐最小可运行测试（至少覆盖：数据解析、入库、基础接口）

【实现要求】
- 优先保证“能跑通 + 可维护 + 可测试”
- 使用类型标注，核心/复杂逻辑添加中文注释
- 配置项集中管理（例如 API 基础地址、抓取频率、数据库路径）
- 数据层与业务层解耦，禁止把 SQL 拼接散落在各模块
- 对外部 API 做超时、重试、错误分类处理
- 数据库写入需使用事务，保证一致性

【建议目录（可按实际微调）】
- backend/app.py
- backend/config.py
- backend/data_fetcher.py
- backend/database.py
- backend/models.py
- backend/routes.py
- scripts/init_db.py
- scripts/test_api.py
- tests/test_fetcher.py
- tests/test_api.py
- requirements.txt
- README.md

【交付内容】
1) 先输出“实施方案摘要”（<=12行）
2) 再按“文件路径 + 完整文件内容”给出新增/修改文件
3) 提供可直接执行的验证步骤（安装、初始化、运行、测试）
4) 提供阶段验收结论：是否达到 M1，若未达到列出缺口

【验收标准（必须全部满足）】
- 能从公开 API 拉取价格并成功落库
- 三张核心表可创建并可读写
- 基础 REST API 可返回有效数据
- 抓取任务可手动触发且支持定时
- 测试可执行且关键路径通过
- README 包含本阶段运行说明与已知限制

【风险与回退策略】
- 若公开 API 不可用：实现 Mock 数据源开关，确保链路可测试
- 若鉴权字段缺失：保留扩展接口，先完成匿名可用字段链路
- 若框架选择存在争议：默认 Flask，补充迁移到 FastAPI 的差异清单

请直接开始执行，不要只给概念性建议。
PROMPT END
```
