# 阶段4集成测试报告（M4）

## 1. 测试范围

本次覆盖“采集入库 -> 后端分析 -> 前端展示 -> 持仓提醒”全链路，并补充异常流与边界流验证，目标是确认系统在本地可稳定交付使用。

## 2. 执行环境

- 操作系统：Windows
- 后端：Flask + SQLite
- 前端：Vue3 + Vite
- 测试框架：pytest

## 3. 集成测试清单与结果

| 用例ID | 场景 | 覆盖路径 | 预期 | 结果 |
| --- | --- | --- | --- | --- |
| IT-01 | 正常全链路 | 抓取、最新价、历史、涨跌榜、持仓、提醒、分析 | 全链路成功，状态码正确 | 通过 |
| IT-02 | 异常流-空数据分析 | 不存在 ammo_id 执行分析 | 返回 404 + EMPTY_DATA | 通过 |
| IT-03 | 边界流-非法参数 | history days=0 / ranking limit=0 | 返回 422 + INVALID_PARAM | 通过 |
| IT-04 | 异常流-远端失败 | 远端抓取失败且关闭 mock 回退 | 返回 500 + FETCH_ERROR | 通过 |
| IT-05 | 提醒链路可恢复 | 触发提醒后读取并标记已读 | 事件可读取、可已读 | 通过 |

## 4. 执行命令与汇总

```bash
pytest -q
```

执行结果：

- 12 passed in 3.02s
- 无阻断级失败

## 5. 失败修复记录（问题清单 -> 优先级 -> 闭环）

| 问题ID | 优先级 | 问题描述 | 修复动作 | 闭环状态 |
| --- | --- | --- | --- | --- |
| M4-P1 | 高 | 看板7日涨跌幅依赖前端N次历史请求，网络抖动下加载慢 | 新增后端聚合接口 `/api/ammo/change-ranking`，前端改单请求 | 已闭环 |
| M4-P2 | 高 | 持仓列表存在逐条查询最新价导致的N+1查询放大 | 持仓列表改为批量最新价映射，并支持分页参数 | 已闭环 |
| M4-P3 | 中 | 图表快速切换筛选触发重复请求与重复渲染 | 增加历史数据短缓存、防抖刷新、图表采样与缩放优化 | 已闭环 |
| M4-P4 | 中 | 新增持仓需手动输入成本价，操作步骤偏多 | 增加“按现价填充”与现价参考展示 | 已闭环 |

## 6. 风险与应急

- 外部API波动：保留 `MOCK_ON_FAILURE` 回退能力，异常时可继续演示与本地使用。
- AI服务波动：分析模块已内置规则分析回退与频控。
- 环境差异：提供本地一键构建脚本与清晰运行说明，降低部署失败率。
